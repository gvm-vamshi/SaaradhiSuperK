# Skip optional native dependencies (sharp, etc.) — these are not needed
# at dev/build time and fail to install on StackBlitz's WebContainer.
# Vercel will install these normally; their absence is fine for both.
optional=false

# Avoid noisy warnings from Next.js's audit step
audit=false
fund=false
